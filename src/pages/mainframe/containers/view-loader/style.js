import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(theme => ({
  logo: {
    marginBottom: '3em'
  },

  statusShower: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column'
  },

  statusContainer: {
    margin: 'auto'
  }
}));
